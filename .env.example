# =============================================================================
# COINBASE DEVELOPER PLATFORM (CDP) CONFIGURATION
# =============================================================================
# CDP Project ID for Embedded Wallets
NEXT_PUBLIC_CDP_PROJECT_ID=your-cdp-project-id

# CDP API Keys (for server-side operations, Paymaster, etc.)
CDP_API_KEY_ID=your-cdp-api-key
CDP_API_KEY_SECRET=your-cdp-api-secret

# =============================================================================
# X402 PAYMENT PROTOCOL CONFIGURATION
# =============================================================================
# The CDP facilitator (from @coinbase/x402) is used for x402 payments
# It connects to x402.org with CDP authentication for settlement operations
# No additional configuration needed - uses CDP_API_KEY_ID and CDP_API_KEY_SECRET above
# Handles gas fees and executes USDC transfers on Base Sepolia testnet

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Base URL for your application (required for API calls and sharing)
NEXT_PUBLIC_BASE_URL=your-URL

# USDC Contract on Base Sepolia Testnet (for x402 payments)
NEXT_PUBLIC_USDC_BASE_CONTRACT=0x036CbD53842c5426634e7929541eC2318f3dCF7e

# storing pfp images
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MongoDB connection string
MONGODB_URI=you-mongodb-URI